# Site map

I use this file as the quick map for the static site.

- `index.html` holds the page content.
- `styles.css` holds layout and visual style.
- `script.js` handles repo filters.
- `assets` holds the AbzumsAI logo.
- `docs` holds site notes.
- `scripts` holds local checks.
